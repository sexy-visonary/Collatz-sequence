print('Please type a number.')
number = int(input())


def collatz(number):
    while number != 1:
        if number % 2 == 0:
            print(number // 2)
            number = number // 2
        else:
            print(number * 3 + 1)
            number = number * 3 + 1

collatz(number)
